REPORT ztest_adjacent_duplicate_fail.

START-OF-SELECTION.
  SELECT a~matnr,
         b~werks
    FROM mara AS a
    INNER JOIN marc AS b
      ON b~matnr = a~matnr
    INTO TABLE @DATA(gt_mat_plant).

  LOOP AT gt_mat_plant INTO DATA(gs_mat_plant).
    WRITE: / gs_mat_plant-matnr, gs_mat_plant-werks.
  ENDLOOP.
